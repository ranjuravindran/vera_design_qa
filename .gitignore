# macOS
.DS_Store

# Dart/Flutter build artifacts
.dart_tool/
.flutter-plugins-dependencies
.packages
/build/
**/doc/api/

# Package convention: don't pin transitive deps for a library others depend
# on (unlike apps, where pubspec.lock is normally committed - see
# example/.gitignore and companion/.gitignore for those).
/pubspec.lock

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/

# Claude Code local settings
.claude/
